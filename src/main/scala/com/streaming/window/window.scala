package com.streaming.window

object window {
  def main(args: Array[String]): Unit = {
//    val ins = List(1,2,3,4,5,6,78,9)
//    val iter: Iterator[List[Int]] = ins.sliding(3,3)
//    for(i <- iter){
//      println(i.mkString("，"))
//    }
  }

}
